# Mandala App

## EAS Build Setup
1. Create account at expo.dev
2. Run: npx eas login
3. Run: npx eas build:configure
4. Add environment variables in Expo dashboard
5. Run: npx eas build --platform android --profile preview

## Environment Variables
Copy .env.example to .env and fill in real values.
Never commit .env to git.
